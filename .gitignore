node_modules
dist
.DS_Store
.vercel
.env
.env*
.secure/
.env*.local

# Secrets drop file (belt-and-suspenders; real location is ~/testforge/keys.md outside the repo)
keys.md

# Private showcase report — dkubex is a PRIVATE repo; keep its audit local only
src/data/showcaseReports/dkubex.json
