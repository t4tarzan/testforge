{
  "name": "fixture-test-quality",
  "version": "0.0.1",
  "description": "Test quality fixture — skipped / focused / assertionless / empty / isolated tests.",
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}
