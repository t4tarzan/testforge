// FIXTURE — imports only the framework, no source files.
// Real-world version: a leftover scaffold or a file someone forgot to delete.
import { it, expect } from 'vitest';

it('framework works', () => {
  expect(1).toBe(1);
});
